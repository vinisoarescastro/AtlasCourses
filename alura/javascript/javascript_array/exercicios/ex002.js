const clinica = [];

clinica.push('Dog Lili');
clinica.push('Dog Frodo');
clinica.push('Dog Felix');

console.log(`Lista de animais em espera na clínica: ${clinica}`);

clinica.pop();
clinica.pop();
clinica.pop();

console.log(`Lista de animais em espera na clínica: ${clinica}`);
