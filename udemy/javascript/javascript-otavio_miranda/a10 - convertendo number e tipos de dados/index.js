let num1 = 17.894511;       // number
let num2 = 2.5;             // number
let temp = num1 * 'Olá';    // NaN - Not a Number

/*
** let num1 = num1.toString();                          // convertendo num1 para string
** console.log(num1.toString() + num2);                 // realiza a concatenação de num1 e num2. string + number = string
** console.log(num1.toString(2));                       // converte num1 para binário
** console.log(num1.toFixed(2));                        // limita o número de casas decimais de num1 para 2
** console.log(Number.isInteger(num1));                 // verifica se num1 é um número inteiro. No caso de número com numero com ponto flutuante, retorna false.
** console.log(Number.isNaN(temp));                     // verifica se temp é um NaN. Retorna true se for NaN e false se não for NaN.
*/


console.log(Number.isNaN(temp));                               // verifica se temp é um NaN. Retorna true se for NaN e false se não for NaN.

